project Default is

   package Compiler is
      for Switches ("Ada") use ("-g", "-O2");
   end Compiler;

   for Main use ("concurrente.adb", "secuencial.adb", "sopa.adb", "sopa.ads");

end Default;

